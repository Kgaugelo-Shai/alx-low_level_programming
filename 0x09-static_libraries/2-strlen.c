#include "main.h"
#include <string.h>
/**
 * _strlen - returns the length of string
 * @s: the string to be examined
 *
 * Return: an integer which represents the length
 */
int _strlen(char *s)
{
	int length;

	length = strlen(s);
	return (length);
}
