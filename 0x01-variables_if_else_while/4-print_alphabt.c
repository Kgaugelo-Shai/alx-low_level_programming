#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: always 0
 */
int main(void)
{
	char c = 'a';

	while (c <= 'z')
	{
		if ((c != q) && (c != e))
			putchar(c);
			c++;
		else
			c++;
	}
	putchar('\n');
	return (0);
}
